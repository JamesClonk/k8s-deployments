#@ load("@ytt:data", "data")
#@ load("@ytt:assert", "assert")
#@ load("_ytt.lib.yml", "labels")

#@ if len(data.values.minecraft.rcon_password) == 0 or data.values.minecraft.rcon_password == "password":
#@  assert.fail("minecraft.rcon_password is missing or invalid")
#@ end
---
apiVersion: v1
kind: Secret
metadata:
  name: minecraft
  namespace: minecraft
  labels: #@ labels("secret")
  annotations:
    kapp.k14s.io/versioned: ""
type: Opaque
stringData:
  RCON_PASSWORD: #@ data.values.minecraft.rcon_password
