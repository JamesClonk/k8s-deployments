#@ load("_ytt.lib.yml", "labels")

---
apiVersion: v1
kind: Service
metadata:
  name: backman
  namespace: backman
  labels: #@ labels("svc")
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector: #@ labels("gui")
